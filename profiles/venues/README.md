# Venue profiles

Create profiles only for venues the project actually targets. Venue rules change, so every profile needs a year and an official-source verification date.

Do not infer current page limits, rebuttal rules, anonymity requirements, or artifact policies from an older profile. Start from `venue-profile.template.yaml`, verify official instructions, and record the supporting links.
